spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: root
        url: jdbc:mysql://localhost:3300/DB_TEST?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
        password: 'root'
    jpa:
        database-platform: org.hibernate.dialect.MySQLDialect
        hibernate:
            ddl-auto: update
server:
    port: '8080'
